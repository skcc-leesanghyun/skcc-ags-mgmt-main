package com.skcc.ags.audit.domain;

/**
 * Enum representing the types of audit records.
 */
public enum AuditType {
    ONBOARDING,    // Personnel joining a project
    OFFBOARDING    // Personnel leaving a project
} 